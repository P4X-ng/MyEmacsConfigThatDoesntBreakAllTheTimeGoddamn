# Environment Variables Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env file to version control

# =============================================================================
# LLM/AI Configuration
# =============================================================================

# OpenAI API Key (required for GPTel and IDE Server chat features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Base URL (optional, for custom endpoints or local LLMs)
# Default: https://api.openai.com/v1
# For local vLLM: http://localhost:8000/v1
# For local TGI: http://localhost:8080/v1
#OPENAI_BASE_URL=https://api.openai.com/v1

# GPTel Backend Selection (optional)
# Options: openai (default), vllm, tgi
# Use "vllm" for local vLLM server
# Use "tgi" for local Text Generation Inference server
#GPTEL_BACKEND=openai

# GPTel Model Selection (optional)
# Default: gpt-4o-mini
# Options: gpt-4, gpt-4-turbo, gpt-3.5-turbo, or your local model name
#GPTEL_MODEL=gpt-4o-mini

# =============================================================================
# Python Development Configuration
# =============================================================================

# Python Virtual Environment Path (optional)
# If not set, will auto-detect .venv or venv in current directory
#VIRTUAL_ENV=/path/to/your/venv

# Jedi Cache Directory (optional)
# Default: ~/.cache/jedi
#JEDI_CACHE_DIRECTORY=~/.cache/jedi

# Jedi Settings File (optional)
# Path to custom Jedi configuration JSON file
#JEDI_SETTINGS=/path/to/jedi-config.json

# Python LSP Server Settings (optional)
# Path to custom PyLSP configuration JSON file
#PYLSP_SETTINGS=/path/to/pylsp-config.json

# =============================================================================
# IDE Server Configuration
# =============================================================================

# IDE Server Host (optional)
# Default: 127.0.0.1 (localhost only for security)
#IDE_SERVER_HOST=127.0.0.1

# IDE Server Port (optional)
# Default: 9999
#IDE_SERVER_PORT=9999

# Rate Limiting Configuration (optional)
# Maximum requests per window (default: 100)
#IDE_SERVER_MAX_REQUESTS=100

# Rate limiting time window in seconds (default: 60)
#IDE_SERVER_RATE_LIMIT_WINDOW=60

# =============================================================================
# Language Server Configuration
# =============================================================================

# Clangd (C/C++) binary path (optional, if not in PATH)
#CLANGD_PATH=/usr/bin/clangd

# Bash Language Server binary path (optional, if not in PATH)
#BASH_LANGUAGE_SERVER_PATH=/usr/local/bin/bash-language-server

# TypeScript Language Server binary path (optional, if not in PATH)
#TYPESCRIPT_LANGUAGE_SERVER_PATH=/usr/local/bin/typescript-language-server

# =============================================================================
# AWS Configuration (for cloud deployment)
# =============================================================================

# AWS Access Key ID (required for AWS deployments)
#AWS_ACCESS_KEY_ID=your_aws_access_key_here

# AWS Secret Access Key (required for AWS deployments)
#AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# AWS Region (optional)
# Default: us-east-1
#AWS_DEFAULT_REGION=us-east-1

# AWS S3 Bucket for backups (optional)
#AWS_S3_BACKUP_BUCKET=my-emacs-config-backup

# =============================================================================
# Development and Debug Configuration
# =============================================================================

# Enable debug logging (optional)
# Set to "true" to enable verbose logging
#DEBUG=false

# Log Level (optional)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
#LOG_LEVEL=INFO

# Log File Path (optional)
# Default: ./logs/ide-server.log
#LOG_FILE=./logs/ide-server.log

# =============================================================================
# Docker Configuration
# =============================================================================

# Docker Image Name for Jedi container (optional)
#JEDI_DOCKER_IMAGE=reliable-jedi:latest

# Docker Registry (optional, for pushing images)
#DOCKER_REGISTRY=your-registry.example.com

# =============================================================================
# Git Configuration
# =============================================================================

# Git User Name (optional, for automated commits)
#GIT_USER_NAME=Your Name

# Git User Email (optional, for automated commits)
#GIT_USER_EMAIL=your.email@example.com

# =============================================================================
# Security Notes
# =============================================================================
# 
# 1. NEVER commit this file with real credentials
# 2. Add .env to your .gitignore file
# 3. Use different keys for development and production
# 4. Rotate API keys regularly
# 5. Use AWS Secrets Manager or similar for production deployments
# 6. Keep API keys with minimal required permissions
# 7. Monitor API usage for suspicious activity
# 
# =============================================================================

# =============================================================================
# Quick Setup Guide
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and add your API keys
# 3. Source the file: source .env (or use direnv)
# 4. Start Emacs: emacs
# 5. Verify: Check that GPTel works with C-c C-g
#
# For IDE Server:
# 1. Set OPENAI_API_KEY in .env
# 2. Start server: cd dot.emacs.d/ide-server && ./start-server.sh
# 3. Server will be available at http://127.0.0.1:9999
#
# =============================================================================
